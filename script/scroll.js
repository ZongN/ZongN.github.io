// 滾輪事件
function OnScroll_Event() {
    var scrollVal = document.getElementsByTagName("section").scrollTop;

    if (scrollVal > 800) {
      console.log(scrollVal);
    }
    else {
      console.log(scrollVal);
    }
}
